# dotfiles

Terminal setup for macOS — WezTerm, Neovim, Powerlevel10k, eza, zoxide, and zsh plugins.

## Usage

```bash
git clone https://github.com/<your-username>/dotfiles.git
cd dotfiles
make setup
```

That's it. After it finishes, open WezTerm and the Powerlevel10k wizard will launch automatically.